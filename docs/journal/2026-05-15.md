# 2026-05-15 — Sprint S-MENU end-to-end

**Outcome:** Gastronomy auto-discovery pipeline shipped Day 1 → Day 3.1.3 single day. 11 sub-sprints, 10 commits, ~25 file edits. Live verified Fortuna case (NIP 8381175797) full-auto → 100 Restaumatic dishes extracted без manual intervention.

## Chronological log

### Morning — Sprint S-CEIDG-DETAILS Day 1 (commit `6f7d622`)

**Trigger:** Earlier audit (2026-05-13) identified `lib/ceidg/client.ts:getFirmDetails(uuid)` as dead code — never called from `lookup/route.ts`. MARCIN BOROWY (NIP 1250825446) case showed Score 5/100 because uprawnienia text `BAR KEMER KEBAB UL. MAGICZNA 6 LOK.1A, 03-289 WARSZAWA` never fetched.

**Actions:**
- Migration `scripts/067_clients_ceidg_id.sql` — `+ceidg_id UUID`, `+brand_aliases JSONB`
- `lib/intelligence/extract-koncesje.ts` (new, ~140 LOC) — Format A regex parser (BUSINESS_KIND_RE anchored), 14/14 inline smoke tests
- `lib/ceidg/client.ts` + `lib/ceidg/types.ts` — added `nip?: string` до `CeidgFilters`, verified `/firmy?nip=` filter live
- `app/api/intelligence/lookup/route.ts` STEP 6.4 — wire `runCeidgDetailsStep()` BEFORE STEP 6.5 AI (так brand_aliases у ctx)
- `lib/ai/business-analysis.ts` — `CompanyContext.brand_aliases` field + prompt block "Marki / nazwy handlowe (z koncesji CEIDG)" з PKD-override wskazówka
- `lib/enrichment/web-search.ts` — Tavily 3rd query for regulamin/polityka pages
- `scripts/diag-ceidg-details.ts` — multi-NIP test CLI

**TS regression hotfix:** `tavily_api_key` regression у runPhaseB params type (missed field when додавав ceidg_api_key). Single-line fix.

### Apify 30s hard timeout (commit `9ccb601`)

**Trigger:** MARCIN BOROWY live re-analyze showed Apify_GMaps stuck `status='running'` @ 12:02:25 (2 hours later, Vercel function timed out at 120s ceiling — STEP 6.4 CEIDG_details + STEP 6.5 AI never reached).

**Discussion:** Vadym chose Option C (Apify timeout) над Option A (re-order Phase B). Reason — Option A regression for ~100+ cohort 29 sp.z o.o. analyses що successfully use GMaps. Timeout pattern preserves GMaps win-case AND ensures CEIDG+AI run when Apify hangs.

**Implementation:** Promise.race(30s) wrapper у `lib/enrichment/apify.ts`. `REQUEST_TIMEOUT_MS` 240_000 → 25_000 (inner). Returns `status='partial'` з `error='APIFY_TIMEOUT_30S'`. route.ts handles partial gracefully.

### Sprint S-MENU Day 2 (commit `6d47b95`)

**Trigger:** Post-CEIDG-Day-1 audit на MARCIN BOROWY — pipeline classified gastronomia + Kemer Kebab brand ✓, але "Menu klienta: Brak menu" + "Prognoza miesięcznej potrzeby" template з 0g for all ingredients. Investigation revealed 3 layered bugs.

**Day 2 #1 — Restaumatic JSON-LD extractor (~340 LOC):**
- `lib/enrichment/restaumatic-menu.ts` (new) — parallel fetch [homepage, /sitemap.xml] @ 12s each, detect `'restaumatic'` signature, walk `schema.org/Restaurant` JSON-LD з `hasMenu`, emit dishes з name/description/price/section
- Promise.race(20s) outer safety net
- $0 cost (just HTTP fetches, no AI)
- Live verified on kemerkebab.pl: 41 dishes у 7 sections — sitemap fallback path works

**Day 2 #2 — Phase B step order fix:**
- Wrap existing STEP 5.5 (www_menu/wolt/wedo) + STEP 5.6 (krs-fullnames) у async closure `runDeferredMenuAndKrs()`
- Invoke AFTER STEP 6.5 AI_business_analysis instead of before
- Reason: gates `isGastronomia`/`isHurtowniaLike` read STALE `business_profile.client_type` (Day 5 sprint precedence issue) — JDG-gastronomy clients classified post-CEIDG missed www_menu step

**Day 2 #3 — Volume baseline fallback:**
- `lib/predictions/restaurant-volume.ts` — `BASELINE_VISITS_PER_LOCATION` map (kebabnia 500, sushi_bar 350, pizzeria 600, kawiarnia 800, fine_dining 200, inne 400)
- New `confidence_tier: 'baseline_no_traffic_data' | 'gmaps_reviews'` field
- Coverage tier = 'baseline_no_traffic_data' коли `gmaps_reviews_count = 0` (e.g. Apify timeout)
- Без цього branch ingredient prediction = 0g (visits_mid множить на 0)

**TS regressions hotfixes:**
- `Type 'string | null' is not assignable to type 'string | undefined'` — line 1281 у lookup/route.ts (`result.error ?? undefined` coercion)
- TS file corruption у virtiofs sandbox (Protocol 37) — defer TSC to Vadym PowerShell

### Day 3 — Brand-aware Tavily + WebsiteOverrideCard UI (commit `cee3088`)

**Trigger:** MARCIN BOROWY case showed Tavily picked `monitorfirm.pb.pl` (Puls Biznesu Monitor — aggregator) AND `yelp.com` (also aggregator) as "company website" across 2 analyses. Day 2 Restaumatic extractor correctly ran ON WRONG URL → "no menu page found". Root cause: AGGREGATOR_BLOCKLIST gap + no brand-aware re-discovery after CEIDG provides brand_aliases.

**Actions:**
- AGGREGATOR_BLOCKLIST +13 entries (monitorfirm.pb.pl, pb.pl, firmy.wp.pl, yelp.com/pl, tripadvisor.com/pl, foursquare.com, etc.)
- `searchCompanyByBrand(brand, city, tavilyKey)` (new exported function) — 15s Promise.race, brand-slug scoring +5 boost
- STEP 6.6 у lookup/route.ts — fires when `brand_aliases.length > 0 AND !manualSet AND (currentWebsite null OR aggregator)`
- POST `/api/clients/[id]/website` (new endpoint) — URL validation, source='manual_override' priority 5
- `WebsiteOverrideCard` UI component — 3-state UI, source badge, "Zapisz і przeanalizuj" sequential POSTs
- SOURCE_PRIORITIES: `manual_override: 5`, `tavily_brand: 2`

### Day 3.2 + Day 3.1 combined commit (`a5d85aa`)

**Trigger:** Post-Day-3 diag — Restaumatic data WAS у DB (65 dishes для MARCIN) but UI showed "Brak menu". Root cause: downstream consumers had stale source filters (`['www_menu', 'wedo_pdf_menu', 'gmaps_menu']` — missed `'restaumatic_menu'`).

**Day 3.2 (downstream consumers):**
- `lib/predictions/aggregate-ingredients.ts` — add `'restaumatic_menu'` до filter + `restaumaticRow` як TOP priority у `chosenDishes`
- `components/clients/menu-section.tsx` — `MenuDishesSource` type + `SOURCE_LABELS_PL` entry
- `app/(dashboard)/clients/[id]/page.tsx` — query filter + dishes selection priority branch
- `app/api/intelligence/lookup/route.ts` — enrichment_log audit log dynamic source post-extraction update

**Day 3.1 (brand-aware Tavily fixes):**
- AGGREGATOR_BLOCKLIST +13 PL gastronomy aggregators (jadlospis.menu, menu.pl, pyszne.pl, foodora.pl, glovoapp.com, bolt.eu, etc.)
- `tavily_brand` priority 2 → 5 (naive Tavily writes under `source='WWW'` priority 4, so tavily_brand=2 був IGNORED)
- City extraction з `brand_aliases[0].address` regex
- Mirror successful website до `clients.website` canonical column

**TS hotfix:** `predictions-section.tsx` `DishesSource` union — Day 3.2 missed updating це duplicate type (`SOURCE_LABELS Record<DishesSource>` required exhaustive coverage).

### Day 3.1.1 — AI extracted_brand fallback (commit `95c575c`)

**Trigger:** Live verify на NIP 8381175797 "Dariusz Wieczorek Fortuna" — brand_aliases empty (no alcohol koncesja). Day 3.1 brand-aware Tavily gated на `brand_aliases.length > 0` → skipped entirely for ~70% gastronomy без koncesji.

**Discussion:** Vadym chose Option D (AI fallback integrated into existing AI_business_analysis step — zero additional API cost). Memory note added: "brand discovery це UNIVERSAL Sztab feature, не CzM-specific".

**Actions:**
- `BusinessProfile` interface +2 fields: `extracted_brand?: string | null`, `extracted_brand_confidence?: 'high' | 'medium' | 'low' | null`
- SYSTEM_PROMPT extended з 8 example titles + extraction rules
- STEP 6.6 cascading lookup — `ceidg_koncesja` priority 1 → `ai_extracted` priority 2 fallback (high/medium only)
- AGGREGATOR_BLOCKLIST +21 PL news domains (rp.pl, wp.pl, gazeta.pl, tvn24.pl, money.pl etc.)
- `sanitizeBrandForQuery()` strips parens before Tavily query
- Multi-token scoring: 2-of-N token match required для +5 boost (rejects "kowalski.pl" pure-surname picks)

**TS hotfix:** TS1005 unterminated string literal — unescaped backticks `` `nazwa` `` всередині template literal SYSTEM_PROMPT. Single-char fix → single quotes.

### Day 3.1.2 — Format B koncesja parser + backfill (commit `38b7d06`)

**Trigger:** Vadym manually checked Fortuna's CEIDG entry — has koncesja "lokal gastronomiczny 'Fabryka sushi', ul. Skarbka z Gór 15U lok. U9, 03-287 Warszawa". Day 1 extractor (Format A anchored на uppercase BUSINESS_KIND_RE) missed Format B pattern. AI fallback then guessed wrong "Fortuna" (це owner suffix у legal title, не real brand).

**Actions:**
- `LOKAL_GASTRO_RE` regex з 6 quote variants (straight + curly + smart)
- Universal `POSTAL_CITY_RE` для both formats
- `BrandAlias` interface +`kind?`, +`city?`, +`postal_code?` optional fields (backward compat)
- AI prompt extended з PRIORYTET block — CEIDG koncesja brand overrides title-derived guess
- `scripts/reextract-brand-aliases.ts` CLI — historical backfill via direct CEIDG re-fetch
- 7/7 inline smoke tests pass

### Day 3.1.2 hotfix — backfill filter bugs (commit `5e1ddad`)

**Trigger:** Backfill `--dry` returned 0 candidates. Diag revealed 2 bugs:

1. `clients.entity_type` column stores `'client'/'prospect'` CRM scope, NOT legal form `'JDG'/'sp.z o.o.'`. Filter `.eq('entity_type', 'JDG')` matched 0 rows ever.
2. PostgREST `.eq('brand_aliases', '[]')` jsonb literal compare returns 0 rows навіть коли empty arrays exist.

**Fix:** Removed both filters. Use `ceidg_id IS NOT NULL` as JDG proxy. Client-side post-fetch filter: `Array.isArray(c.brand_aliases) && c.brand_aliases.length === 0`.

**Confirmation:** Live count showed 3 JDG candidates (MARCIN already populated, Fortuna + DEKOB empty — target backfill scope).

### Day 3.1.2.1 — Stale tavily_brand website invalidation (commit `389d954`)

**Trigger:** After backfill ran, Fortuna re-analyze STEP 6.6 didn't fire. Diag revealed: previous wrong-brand tavily_brand row (`https://fortuna.info.pl` — Pensjonat Fortuna, не Fabryka Sushi) still active. Gate checks "is current website aggregator" — fortuna.info.pl wasn't blocklisted (приватний small site), gate skipped.

**Discussion:** Option D chosen — extend backfill script to ALSO invalidate stale tavily_brand websites коли brand_aliases changes. Decisions confirmed:
1. `superseded_by_source = 'manual_backfill_invalidation'` (distinctive audit label)
2. Defensive canonical clear (only-if-matches superseded value)
3. `--force` flag = process ALL clients regardless brand_aliases state, invalidate stale website regardless of brand-change

**Actions:**
- `invalidateStaleTavilyBrandWebsite()` helper — mirrors `merge.ts:158-165` supersede pattern
- CLI flags `--force` + `--skip-invalidation`
- Dedupe detection via `JSON.stringify` — `[NO CHANGE]` audit log коли brand_aliases identical
- New counters: `noChange`, `websitesSuperseded`, `canonicalsCleared`

**Verification:** Vadym ran `--force` → `superseded=1, canonical_cleared=1` for Fortuna. company_profile_fields[website] tavily_brand row marked SUPERSEDED by 'manual_backfill_invalidation'. clients.website cleared (NULL).

### Day 3.1.3 — Drop site:.pl from Tavily query (commit `2f11d98`)

**Trigger:** Fortuna re-analyze post-Day-3.1.2.1 — STEP 6.6 DID fire (gate worked), but Tavily returned `candidates_considered: 0`. Diag revealed Tavily query format issue.

**Live Tavily probe:**
- `"FABRYKA SUSHI" Warszawa menu OR oferta OR jadlospis site:.pl` → **0 results**
- `"FABRYKA SUSHI" Warszawa menu OR oferta OR jadlospis` (no site:) → **5 results** включаючи real Fabryka Sushi

**Conclusion:** Tavily НЕ honors Google-style `site:` operator. Day 3 original implementation був wishful thinking.

**Actions:**
- Removed `site:.pl` suffix з query string (single-line fix)
- `BrandSearchResult` interface +`query_sent: string | null` field
- Both success + partial branches log `city` + `query_sent` у raw_payload (debug visibility)

**TS hotfix:** TS2345 у outer Promise.race timeout fallback (line 597) — separate object literal didn't include new `query_sent` field. Added `query_sent: null`.

### Final live verification — Fortuna FULL AUTO

Re-analyze fired @ 19:41:52.

**Result:**
```
clients.website (canonical):  https://www.fabrykasushi.com  ✓ REAL SITE
brand_aliases:                 [{brand:"FABRYKA SUSHI", kind:"LOKAL GASTRONOMICZNY",
                                 city:"Warszawa", source:"ceidg_koncesje",
                                 address:"ul. Skarbka z Gór 15U lok. U9, 03-287 Warszawa",
                                 postal_code:"03-287"}]
business_profile.client_subtype:        sushi_bar
business_profile.buyer_strength_for_chm: 5  (correct — sushi не fit CzM kiszonki)
business_profile.extracted_brand:        "FABRYKA SUSHI" (high)

contact_enrichment[restaumatic_menu]:
  status:              SUCCESS
  website:             https://www.fabrykasushi.com
  dishes:              100  ← !!!
  extraction_source:   restaumatic_jsonld
  matched_path:        /  (root page має hasMenu — single-location)

company_profile_fields[website]:
  ★ ACTIVE     19:41:54  src=tavily_brand  prio=5  value=https://www.fabrykasushi.com
  SUPERSEDED   17:32:10  src=tavily_brand  prio=5  value=https://fortuna.info.pl
                                                    by=manual_backfill_invalidation
  SUPERSEDED   16:15:21  src=WWW           prio=4  value=https://www.rp.pl
                                                    by=tavily_brand
```

**100 dishes extracted end-to-end автоматично без manual override.** Sprint S-MENU goal achieved.

## Cross-references до updated docs sections

- **`docs/sztab-state.md`** — appended 2026-05-15 section з pipeline architecture, brand discovery cascade, AGGREGATOR_BLOCKLIST overview, per-product matching principle, manual override UI, data sources operational, live verification cases
- **`docs/sztab-protocols.md`** — appended cleanup backlog (5 items) + 7 lessons learned (Tavily syntax ≠ Google, CEIDG koncesja authoritative, multi-format parser, stale row invalidation, universal layer principle, PostgREST jsonb gotcha, entity_type ≠ legal form)
- **`docs/sztab-sprints.md`** — appended Sprint S-MENU section з day-by-day breakdown + 2 live verification cases

## Memory entries added (Claude memory hygiene per Protocol 5)

Universal principles (live forever у Claude memory):
1. CEIDG koncesja brand завжди authoritative над title-derived AI guess (NEW)
2. Tavily search ≠ Google syntax — always probe live perш ніж assume operators work (NEW)
3. Universal intelligence layer vs per-product matching = separate concerns. Phase B = shared. /produkty/[id] = product-specific (CLARIFIED — was implicit, now explicit)
4. Per Protocol 8 — API keys live у `params` table, не process.env (REINFORCED по Day 1 lookup/route.ts findings)
5. CEIDG covers only JDG. Use `clients.ceidg_id IS NOT NULL` як JDG proxy filter (DB column `entity_type` ≠ legal form) (NEW)

Tactical principles (replaced/refined):
- Phase B step ordering: AI runs BEFORE downstream menu/krs steps (REPLACES Day-2-vintage "AI у STEP 6.5 — все одно") (REFINED)
- Aggregator blocklist coverage = top PL news + food delivery + business directories + registry sites + reviews (~70 domains total) (NEW)

## Key Vadym corrections

1. **Per-product matching architecture** (early Day 3.1.1) — "memory note: brand discovery це UNIVERSAL Sztab feature (не CzM-specific). Майбутні products (SpoonJoy, Karol wędliny, Cezary овочі) теж залежать від website→menu→ingredients pipeline. Day 3.1.1 — foundation для всіх продуктових потоків."
2. **Cowork prompt format** (mid-day, Protocol 40 reinforcement) — "GO Day 3.1.2 — Decisions confirmed [N items]" pattern як standard для approved-without-modification spec execution.
3. **Defensive canonical clear** (Day 3.1.2.1) — "Only-if-matches superseded value. Не clear коли value != superseded value. Зберігає Vadym-у manual override якщо він робив після останнього STEP 6.6."

## Commit timeline (git log)

```
2f11d98  fix(intelligence): drop site:.pl from Tavily query (returned 0 results)
389d954  feat(scripts): Day 3.1.2.1 — invalidate stale tavily_brand websites
5e1ddad  fix(scripts): reextract-brand-aliases filter logic — entity_type + jsonb bugs
38b7d06  fix(intelligence): Format B koncesja parser + AI priority + backfill script
95c575c  feat(intelligence): AI-extracted brand fallback for STEP 6.6 (Day 3.1.1)
a5d85aa  fix(intelligence): Restaumatic downstream readers + brand-aware Tavily wins
cee3088  feat(intelligence): brand-aware website discovery + manual override UI
6d47b95  feat(menu): Restaumatic JSON-LD extractor + Phase B ordering + volume baseline fallback
9ccb601  fix(apify): hard 30s timeout via Promise.race to protect Phase B budget
6f7d622  feat(ceidg-details): JDG koncesje brand_aliases for AI ctx — closes MARCIN BOROWY case
```

10 commits Sprint S-MENU. Plus ~5-6 hotfix commits для TS regressions (compiled separately per Protocol 14 — Vadym committs).

## Next sprint (open questions)

- Day 4+ cleanup backlog items (DishesSource consolidation, factory helpers, sp.z o.o. KRS short-name extraction, city extraction chain expansion, Apify async investigation) — per docs/sztab-protocols.md
- DEKOB / OKEH BAKERY re-analiza verification (third backfill candidate from Day 3.1.2 dataset)
- Mass test 20+ JDG gastronomy re-analyses — measure ≥95% auto-discovery rate hypothesis
- Sprint S-CORE.3.B (per-product matching architecture) — depends on completing universal layer (DONE), now може start
